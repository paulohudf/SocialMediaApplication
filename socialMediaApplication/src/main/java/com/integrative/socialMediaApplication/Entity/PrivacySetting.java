package com.integrative.socialMediaApplication.Entity;

public enum PrivacySetting {
    PUBLIC,      // Visible to everyone
    FRIENDS_ONLY, // Visible only to friends/connections
    PRIVATE      // Visible only to the user
}
